name: example
description: A project used to demo the sealed class generator
version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sealed_class:
    git: 
      url: https://github.com/martirius/sealed_class_generator
      ref: master
      path: ./sealed_class
dev_dependencies:
  build_runner: ^1.7.1
  flutter_test:
    sdk: flutter
  sealed_class_generator:
    git: 
      url: https://github.com/martirius/sealed_class_generator
      ref: master
      path: ./sealed_class_generator

publish_to: none